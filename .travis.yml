language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t svidalikerlan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true svidalikerlan/docker-react npm run test -- --coverage
